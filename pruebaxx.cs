using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Leap.Unity;

public class pruebaxx : MonoBehaviour
{
    public Transform pulgar_;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //RigidFinger ff = pulgar_.gameObject.GetComponentInParent<RigidFinger>();

       /* Vector3 aa = new Vector3(0f, 0f, 1f);        

        //transform.rotation = Quaternion.LookRotation(Vector3.Scale(ff.GetBoneDirection(2), aa) *.2f);
        */

        //transform.rotation = Quaternion.LookRotation(ff.GetBoneDirection(2) * .6f);

    }
}
